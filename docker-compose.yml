version: '3.8'

services:
  backend:
    build:
      context: ./backend
    container_name: docmind-api
    ports:
      - "8001:8001"
    volumes:
      - docmind_data:/app/data
      - ./backend/.env:/app/.env:ro
    restart: unless-stopped

volumes:
  docmind_data:
    driver: local
